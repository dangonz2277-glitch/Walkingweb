# Punto de reanudación — Orden 08 (15-09-2026)

Trabajo pausado por petición de Daniel. No hacer push ni despliegue. La Orden 08 continúa abierta; la entrega 08C de Antigravity está en el árbol de trabajo, sin commit nuevo. `HEAD` sigue en `41c0930`.

Verificación de Codex al pausar: `npm test` pasa 57/57 en 7 archivos, `npm run lint` termina sin advertencias y `git diff --check` pasa. La entrega de Antigravity informa build, pgTAP, concurrencia, gateway y API en verde; Codex no volvió a ejecutar esas cuatro últimas suites en esta revisión. En la revisión anterior, Codex validó en navegador real dos perfiles, igual fecha/total y aislamiento; esa validación no se repitió después de 08C.

Antes de cerrar la Orden 08, revisar tres detalles: la prueba de «respuesta perdida después del guardado» solo simula dos respuestas RPC y no comprueba una fila real ni relectura; la prueba UI de login exitoso debe afirmar el estado visible del reporte y no solo la desaparición del formulario; el test del bundle depende de un build previo y toma la clave de `.env.local`, por lo que hace falta un comando reproducible en CI que ejecute build antes del escaneo y falle si no hay clave configurada para esa comprobación. Verificar también una nueva prueba real del popup con teclado y vista móvil.

Al retomar: inspeccionar el diff sin perder cambios locales, ejecutar `npm run build`, `npm test`, `npm run lint`, `git diff --check`, pgTAP, concurrencia, gateway y API; revisar limpieza de cuentas sintéticas. Cerrar la Orden 08 solo después de resolver los puntos anteriores y documentar el resultado. Mantener SQL/RLS, gateway y catálogo fuera de alcance salvo defecto demostrado.
