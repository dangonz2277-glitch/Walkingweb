# Orden 04 para Antigravity — control de versiones y pruebas de API local

La Orden 03 está cerrada en PostgreSQL local. Antes de conectar React, demuestra la misma seguridad a través de HTTP/PostgREST y deja una línea base Git revisable. Trabaja solo en `/Users/daniel/Documents/Desarollo/Web/WalkingWeb`; no publiques la app ni conectes staging o producción.

1. **Git/CI:** el repositorio local todavía no tiene primer commit. Revisa `git status`, `.gitignore` y `git diff --no-index` o el contenido que vaya a entrar; excluye `.env*`, respaldos, datos locales de Supabase, claves, `node_modules` y `dist`. Conserva `supabase/config.toml`, migraciones y tests. Ajusta CI a una versión Node compatible con Vite 8 y ejecuta el mismo `npm ci`, test, lint y build. Prepara un commit inicial legible en esta raíz, sin forzar push ni reescribir historia. Informa hash del commit y estado del remoto. Si algún archivo contiene secreto, detén el commit y repórtalo.
2. **Pruebas HTTP locales:** usando Supabase local, crea dos usuarios Auth sintéticos y un perfil desactivado. Obtén tokens reales por el flujo Auth local; no simules claims desde una conexión superuser. Con la clave publicable local, prueba `/rest/v1/daily_reports` y `/rest/v1/rpc/set_daily_report`: anónimo no lee/escribe; Alice solo lee lo suyo y puede llamar la RPC; Bob no ve/modifica lo de Alice; desactivado no guarda; un `INSERT`/`UPDATE` directo falla; una revisión obsoleta devuelve conflicto sin cambiar el total. Verifica que un usuario no puede elevar su perfil. Usa solo datos sintéticos y limpia al terminar.
3. **Automatización:** guarda las pruebas de API en el repo para poder repetirlas. No metas claves locales ni tokens en código o logs. Si añadís el stack SQL/API a GitHub Actions, que el job realmente levante Supabase y falle cuando falle pgTAP, la prueba HTTP o concurrencia; no dejes checks verdes que omitan pruebas. Documenta prerequisitos y comandos.
4. **Entrega:** comparte archivos cambiados, comandos, resultados y cualquier limitación. No conectes todavía React, no añadas la contraseña general en el frontend y no uses una base remota.
